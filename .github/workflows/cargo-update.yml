# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/cargo-update.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  updating-cargo:
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@main
      with:
        fetch-depth: 1
        persist-credentials: false
    - name: Install Nix
      uses: nixbuild/nix-quick-install-action@master
    - name: Magic Nix Cache(Use GitHub Actions Cache)
      uses: DeterminateSystems/magic-nix-cache-action@main
    - name: Update Cargo dependencies
      run: nix develop --command cargo update
    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v7
      with:
        body: Automated Cargo dependency update.
        branch: cargo-update
        commit-message: 'chore(deps): update Cargo dependencies'
        title: 'chore(deps): update Cargo dependencies'
    timeout-minutes: 30
'on':
  schedule:
  - cron: 0 0 1-7 * 0
  workflow_dispatch: {}
